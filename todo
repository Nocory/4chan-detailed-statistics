Needs to check if post text changed. (Ban message)
Needs to check if post was deleted.
Needs to check thread that got pushed off the board into 4chan archive one last time.

when going through catalog, check visible and then also visibles from last check that have been pushed off

put tokens and stats in its own DB, so cache doesn't have to hold useless info